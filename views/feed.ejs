<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>RSS Reader</title>
    <script>
    </script>
  </head>
  <body>
    <h1><%= feed.title %></h1>
    <ol start="0">
      <% for (let i = 0; i < feed.items.length; i++) { %>
        <li>
          <a href="/<%= i %>"><%= feed.items[i].title %></a>
          <p><%= new Date(feed.items[i].pubDate) %></p>
        </li>
      <% } %>
    </ol>
  </body>
</html>
